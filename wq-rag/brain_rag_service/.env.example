# brain_rag_service 直接复用 brain_index/.env 的端点配置。
# 如果你想给 MCP 用独立配置（如不同的 collection），在这里 .env 覆盖即可。
#
# 默认行为：本 server 启动时 sys.path 注入 ../brain_index/，然后 `import config`
# 会读取 brain_index/.env。你**通常不需要**单独写 brain_rag_service/.env。

# 仅在想覆盖默认时取消注释：
# BGEM3_BASE_URL=
# QDRANT_HOST=
# QDRANT_PORT=
# QDRANT_COLLECTION=
